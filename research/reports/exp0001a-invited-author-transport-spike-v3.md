# EXP-0001A invited-author transport spike v3

Decision: **pass**.

A fresh ChatGPT-authenticated Codex task using `gpt-5.6-terra` at `medium` ran in a projectless workspace with no repository access, private API access, fork, or shared history. It joined one exact private Jazzboard room through browser-exposed WebMCP, authored an artifact, and returned a terminal result.

The independent room controller proved a five-revision authoritative artifact, exact server-side author attribution, a completed bound visual-inspection program, sanitized semantic state, and a revision-matched PNG. Current Codex `read_thread` omitted nested node-repl result fields, so those results are truthfully recorded as `unobservable`; no result bytes were invented or estimated.

Six earlier disposable attempts remain privately retained and classified `invalid_harness`. None were promoted, replaced, or counted as qualification evidence. This passing spike releases only the fixed three-task model-role qualification. It does not release the 48-attempt experiment.

Public record: `research/data/exp0001a-invited-author-transport-spike-public-v3.json`.
