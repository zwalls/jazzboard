# Large or reproducible research outputs stay outside Git. Commit manifests,
# hashes, annotations, protocols, scripts, and aggregate reports instead.
/data/raw/
/data/derived/
/results/runs/
/artifacts/
/tmp/
